# Runtime dependencies
litellm>=1.50
questionary>=2.0
rich>=13.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
networkx>=3.0

# Phase 1 – executor / tool calling
pexpect>=4.9

# Phase 2 & 3 – memory + database
chromadb>=0.5
sqlalchemy>=2.0
alembic>=1.13

# NimiPlaybook KB – intelligence ingesters & CLI
typer>=0.12
requests>=2.31
tqdm>=4.66
attackcti>=0.3
nvdlib>=0.7
stix2>=3.0
taxii2-client>=2.3

# Dev / test
pytest>=7.4
pytest-mock>=3.12

# Optional: local embedding model (install separately)
# sentence-transformers>=2.7
